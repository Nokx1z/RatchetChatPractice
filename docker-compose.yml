services:
  ratchet-app:
    build: .
    container_name: ratchet-chat
    ports:
      - "8080:8080"
    volumes:
      - .:/app
    working_dir: /app
    stdin_open: true
    tty: true
    command: /bin/bash